# Boxer Adapter Web

이 폴더는 `boxer` 코어와 웹 클라이언트 사이를 잇는 웹 adapter 자리다.

예정 역할:

- 웹 API / BFF
- 세션 관리
- 인증, rate limit
- SSE 또는 WebSocket 스트리밍
- `widget` 요청을 `boxer` core 호출로 변환

현재 단계:

- 폴더와 문서 경계만 생성
- 실제 구현은 이후 단계에서 진행
